declare const element: HTMLElement;
declare const companyName: string;

element.textContent = companyName;
const contact = <a href="mailto:hello@example.com">Email us</a>;
const staticConfig = { canonicalHost: "example.com" };
void contact;
void staticConfig;
export {};
